export const arrayBuiltin = `class Array {
  function int new(int size) {
      var int i;
      let i = 1;
  }

  // Disposes this array
  method void dispose() {
      // do Memory.deAlloc(this);
      // return;
  }
}`;
